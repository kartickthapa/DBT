version: 2 

models:
    - name: stg_customers
      description: one unique customer per row
      columns:  
        - name: customer_id
          description: '{{doc("order_status")}}'
          tests:
            - unique
            - not_null
6